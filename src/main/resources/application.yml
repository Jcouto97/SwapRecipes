server:
  port: ${SERVER_PORT:8080}

spring:
  sql: #
    init: #
    #  mode: always #
  output:
    ansi:
      enabled: ALWAYS
  jpa:
    # defer-datasource-initialization: true #
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        #org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: ${HIBERNATE_DDL_AUTO:create-drop} #create-drop #mandar para deploy mandar com UPDATE
      show-sql: true
  datasource:
    platform: postgres
    url: ${POSTGRES_URL:jdbc:postgresql://ec2-44-198-82-71.compute-1.amazonaws.com:5432/dfph104hoc5h0v}
    username: ${POSTGRES_USER:rpspxawoyczbus}
    password: ${POSTGRES_PASSWORD:0abfbbdd3126490ed52b71efa05cde28b8bdf256a1343b954711eedf1e54c648}
    driverClassName: org.postgresql.Driver